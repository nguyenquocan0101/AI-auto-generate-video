---
description: Implement a planned feature phase by phase. Modes (pick one): --fast (skip test/review), --hard (mandatory reviewers, no auto-approve), --parallel (auto-continue between phases, full test+review at end). Flags: --no-test (skip tester), --tdd (tests-first). Accepts an optional plan file path: /ck:cook plans/260418-auth/plan.md
---

Load the `ck:cook` skill and run it with `$ARGUMENTS`.
